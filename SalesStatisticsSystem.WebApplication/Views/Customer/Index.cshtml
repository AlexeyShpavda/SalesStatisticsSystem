@using SalesStatisticsSystem.WebApplication.Models.Filters
@model IEnumerable<SalesStatisticsSystem.WebApplication.Models.SaleViewModels.CustomerViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>All Customers</h2>

<p>
    @Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-primary" })
</p>

<div id="filterDiv">
    @Html.Partial("Partial/_CustomerFilter", (CustomerFilter) ViewBag.CustomerFilter)
</div>

<div id="updatedTableWithAjax">
    @Html.Partial("~/Views/Customer/Partial/_CustomerTable.cshtml", Model)
</div>